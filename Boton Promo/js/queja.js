function verificarQueja(selectElement) {
    const valorSeleccionado = selectElement.value;
    if (valorSeleccionado === "queja") {
        window.alert("Recuerda que para quejas o reclamos de compra de productos adquiridos debe anexar factura para creerle.");
    }
}